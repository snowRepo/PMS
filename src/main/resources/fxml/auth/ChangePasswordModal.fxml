<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Pos?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.pms.controller.auth.ChangePasswordModalController"
            prefWidth="400" prefHeight="480" style="-fx-background-color: white;">

    <top>
        <VBox alignment="CENTER" spacing="10" style="-fx-padding: 20 20 10 20;">
            <Label text="Change Password" style="-fx-font-size: 20px; -fx-font-weight: bold;"/>
            <Label text="Enter your current PIN to authorize this change." style="-fx-text-fill: #71717a;"/>
        </VBox>
    </top>

    <center>
        <VBox alignment="TOP_CENTER" spacing="24" style="-fx-padding: 20;">
            
            <VBox spacing="8" alignment="CENTER">
                <Label text="CURRENT PIN" style="-fx-font-size: 12px; -fx-font-weight: bold; -fx-text-fill: #52525b;"/>
                <HBox fx:id="currentPinRow" spacing="8" alignment="CENTER"/>
            </VBox>
            
            <Separator maxWidth="300"/>

            <VBox spacing="8" maxWidth="300">
                <Label text="NEW PASSWORD" style="-fx-font-size: 12px; -fx-font-weight: bold; -fx-text-fill: #52525b;"/>
                <PasswordField fx:id="newPassField" promptText="At least 8 characters"/>
            </VBox>

            <VBox spacing="8" maxWidth="300">
                <Label text="CONFIRM NEW PASSWORD" style="-fx-font-size: 12px; -fx-font-weight: bold; -fx-text-fill: #52525b;"/>
                <PasswordField fx:id="confirmPassField" promptText="Confirm password"/>
            </VBox>

            <Label fx:id="errorLabel" textFill="#ef4444" wrapText="true" managed="false" visible="false"/>

        </VBox>
    </center>

    <bottom>
        <HBox spacing="10" alignment="CENTER" style="-fx-padding: 20;">
            <Button text="Cancel" onAction="#handleCancel" prefWidth="100"/>
            <Button text="Change Password" onAction="#handleSave" defaultButton="true" prefWidth="150" />
        </HBox>
    </bottom>
</BorderPane>
